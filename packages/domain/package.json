{
  "name": "@seg/domain",
  "version": "0.1.0",
  "private": true,
  "description": "SEG business rules: identity keys, estimate roll-ups, grid building, upload and export rules. Pure TypeScript, no I/O.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
