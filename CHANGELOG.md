# Changelog — SEG (ABR-SEG)

All notable changes, per the Gen Clover playbook. Each line starts with the work ID.
Versions follow `vMAJOR.MINOR.PATCH`; UAT builds are tagged `vX.Y.Z-uat.N`.

## Unreleased

### Fixed
- ABR-SEG-CHAT-BUG-3: The conversation list preview now follows deletes, admin removals and edits.
  It shows the latest message that's still there (or "No messages yet"), edits show their new
  wording with the original time, and rooms re-sort after their latest message is deleted.

## v1.0.0 — baseline

Production as of 2026-09-25 (commit `e4b15fb`, "Release: Product guide in the admin console"),
taken as the baseline when the Gen Clover process was adopted. Earlier history is in the git log.
