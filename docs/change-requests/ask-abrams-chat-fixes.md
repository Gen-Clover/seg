# Ask Abrams chat fixes — moved to GitHub issues

This document is retired. Its two change requests are now tracked as issues in `Gen-Clover/seg`,
from ABR-SEG-DISC-01, scoped in ABR-SEG-SCOPE-01 (delivery repo `Gen-Clover/abr-delivery`):

| Was | Now | Title |
|---|---|---|
| CR-01 | #2 (`ABR-SEG-CHAT-BUG-2`) | Chat: new users and newly added group members see the whole history as unread |
| CR-02 | #3 (`ABR-SEG-CHAT-BUG-3`) | Chat: conversation list preview still shows deleted, moderated or old edited text |

Work from the issues, not from an older copy of this file. The original write-up is in the git history.
